#!/bin/bash
# FaceMorph launcher for macOS. First time: right-click this file > Open.
# If it won't run, make it executable once:  chmod +x Uruchom_macOS.command
cd "$(dirname "$0")"
python3 FaceMorph.py
